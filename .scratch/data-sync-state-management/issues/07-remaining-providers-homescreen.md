Status: `completed`

## What to build

Complete the migration of all remaining `FutureProvider` instances to `AsyncNotifier` extending `CachedRepository` with appropriate TTL, remove HomeScreen's `initState()` provider invalidation pattern, and add pull-to-refresh support across all screens.

### Current problem (from codebase exploration)

- HomeScreen `initState()` uses `Future.microtask()` to invalidate 3 providers every time the tab is selected: `continueLearningProvider`, `userProgressProvider`, `coursesProvider` — forcing unnecessary API calls
- `coursesProvider` and `courseDetailProvider` are `FutureProvider` with no TTL — refetched on every watch
- `courseDetailProvider`, `moduleDetailProvider` are `FutureProvider.family` with no caching
- No pull-to-refresh mechanism beyond manual `ref.invalidate()`

### What changes

- Migrate `coursesProvider` to AsyncNotifier extending CachedRepository (TTL: 30 minutes) — no DataChangeBus subscription needed (rarely changes)
- Migrate `courseDetailProvider` to family AsyncNotifier extending CachedRepository (TTL: 30 minutes)
- Migrate `moduleDetailProvider` to family AsyncNotifier extending CachedRepository (TTL: 30 minutes)
- `userProfileProvider` subscribes to DataChangeBus tag `'auth'` — auto-refetch on login/logout
- Remove HomeScreen `initState()` invalidation — providers have TTL, `ref.watch()` returns cached data if fresh
- Remove HomeScreen `_onRefresh()` invalidation — replace with provider refresh method (respects TTL)
- Add pull-to-refresh: expose `refresh()` method on CachedRepository that forces refetch regardless of TTL
- Replace all remaining manual `ref.invalidate()` calls in auth-related screens with DataChangeBus `'auth'` event (emitted by AuthNotifier on login/logout)
- Use `@riverpod` and `@freezed` for all new/modified providers and models

## Acceptance criteria

- [x] `coursesProvider` as AsyncNotifier extending CachedRepository (TTL: 30 min)
- [x] `courseDetailProvider` as family AsyncNotifier extending CachedRepository (TTL: 30 min)
- [x] `moduleDetailProvider` as family AsyncNotifier extending CachedRepository (TTL: 30 min)
- [x] `userProfileProvider` subscribes to DataChangeBus tag `'auth'`
- [x] HomeScreen `initState()` no longer invalidates any providers
- [x] HomeScreen `_onRefresh()` uses provider `refresh()` method instead of `ref.invalidate()`
- [x] CachedRepository exposes `refresh()` method that forces refetch regardless of TTL
- [x] AuthNotifier emits `DataChanged(tags: {'auth'})` on login/logout
- [x] No manual `ref.invalidate()` for user-related providers remains in auth screens
- [x] Pull-to-refresh works on HomeScreen, BookmarksScreen, ProfileScreen
- [x] No `FutureProvider` instances remain in the codebase (all migrated to AsyncNotifier)
- [x] Unit tests for CachedRepository `refresh()` method
- [x] End-to-end verification: open HomeScreen → no unnecessary API calls; pull-to-refresh → data refreshes

## Blocked by

- Issue 03 (Bookmark Single Source) — bookmarkStatsProvider migration
- Issue 04 (Lesson Progress + Continue Fix) — continueLearningProvider migration
- Issue 06 (Exercise Play + Stats Sync) — userProgressProvider migration

## Implementation notes

### Files created

- `mobile/lib/features/courses/data/courses_providers.g.dart` — Generated by `riverpod_generator` cho `@riverpod` family providers `CourseDetail` và `ModuleDetail`.

### Files modified

- `mobile/lib/core/sync/cached_repository.dart` — Sửa `CachedRepository.refresh()` và `CachedNotifierMixin.refresh()` để gọi `forceExpire()` trước `ref.invalidateSelf()`, đảm bảo refetch bất chấp TTL. Đổi return type từ `void` sang `Future<void>` để UI có thể `await`.
- `mobile/lib/features/courses/data/courses_providers.dart` — Migrate `coursesProvider` từ `FutureProvider` sang `CoursesNotifier` extends `CachedRepository` (TTL 30min). Migrate `courseDetailProvider` và `moduleDetailProvider` từ `FutureProvider.family` sang `@riverpod` family class với inline TTL cache logic (30min) và method `refresh()`. Giữ nguyên `UserProgressNotifier`.
- `mobile/lib/features/profile/data/profile_providers.dart` — Migrate `userProfileProvider` sang `UserProfileNotifier` extends `AsyncNotifier` + `CachedNotifierMixin` + `DataChangeBusSubscriber` với tag `'auth'` và TTL=0 (ephemeral). Migrate `exerciseStatsProvider` sang `ExerciseStatsNotifier` tương tự với tag `'exercise'`.
- `mobile/lib/features/home/data/home_providers.dart` — Thêm `refresh()` method vào `ContinueLearningNotifier`.
- `mobile/lib/core/providers/auth_state_provider.dart` — Thêm `notifyAuthenticated(bool)` để emit `DataChanged(tags: {'auth'})` khi login. Trong `logout()`, emit `'auth'` event trước khi set state false. Xóa `ref.invalidate(userProfileProvider)`.
- `mobile/lib/core/providers/providers.dart` — Migrate `preferencesProvider` từ `FutureProvider` sang `AsyncNotifierProvider<PreferencesNotifier, PreferencesService>`. Thêm `PreloadedPreferencesNotifier` để dùng trong override cho tests và `main.dart`.
- `mobile/lib/features/home/presentation/screens/home_screen.dart` — Xóa hoàn toàn `initState()` invalidation. `_onRefresh()` giờ gọi `.notifier.refresh()` trên `coursesProvider` và `continueLearningProvider`.
- `mobile/lib/features/profile/presentation/screens/profile_screen.dart` — Bọc body trong `RefreshIndicator`, `_onRefresh` gọi `refresh()` trên `userProfileProvider` và `exerciseStatsProvider`. Nút Retry trong error state cũng dùng `.notifier.refresh()`.
- `mobile/lib/features/auth/presentation/screens/login_screen.dart` — Xóa `ref.invalidate(userProfileProvider)` ở cả login và Google login. Thay `setAuthenticated(true)` bằng `notifyAuthenticated(true)`.
- `mobile/lib/features/auth/presentation/screens/register_screen.dart` — Xóa `ref.invalidate(userProfileProvider)`, thay `setAuthenticated(true)` bằng `notifyAuthenticated(true)`.
- `mobile/lib/features/auth/presentation/screens/email_verification_screen.dart` — Xóa `ref.invalidate(userProfileProvider)`, thay `setAuthenticated(true)` bằng `notifyAuthenticated(true)`.
- `mobile/lib/features/onboarding/presentation/screens/onboarding_screen.dart` — Xóa `ref.invalidate(userProfileProvider)`.
- `mobile/lib/main.dart` — Thay `preferencesProvider.overrideWithValue(AsyncData(...))` bằng `overrideWith(() => PreloadedPreferencesNotifier(...))`.
- `mobile/test/widget_test.dart` — Cập nhật 4 overrides `preferencesProvider` sang dùng `PreloadedPreferencesNotifier`.
- `mobile/test/features/onboarding/presentation/screens/onboarding_screen_test.dart` — Cập nhật override `preferencesProvider` sang dùng `PreloadedPreferencesNotifier`.
- `mobile/test/core/sync/cached_repository_test.dart` — Thêm unit test: `refresh forces refetch regardless of TTL`.
- `mobile/lib/features/home/presentation/widgets/continue_card.dart` — Đổi retry từ `ref.invalidate` sang `ref.read(continueLearningProvider.notifier).refresh()`.
- `mobile/lib/features/courses/presentation/screens/courses_screen.dart` — Đổi retry từ `ref.invalidate` sang `ref.read(coursesProvider.notifier).refresh()`.

### Files deleted

- Không có file nào bị xóa.

### Pipeline notes

- `flutter analyze` pass với 0 error (chỉ còn warnings/info pre-existing).
- `flutter test test/core/sync/` pass 15/15 (7 tests CachedRepository + 8 tests DataChangeBus).
- Widget tests pre-existing (`widget_test.dart`, `onboarding_screen_test.dart`) vẫn fail do lỗi UI/layout không liên quan đến thay đổi này.
- `build_runner` sinh `courses_providers.g.dart` cho `@riverpod` family providers.
